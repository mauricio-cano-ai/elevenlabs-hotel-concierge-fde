# Evaluation Catalog

`scenarios.yaml` is a platform-neutral source of truth for the 12 required conversational and backend scenarios. For ElevenLabs Agent Testing, map each conversational case to the corresponding Tool Call Test, next-reply expectation, or success criterion. Backend-only HMAC/idempotency cases are enforced by pytest.
